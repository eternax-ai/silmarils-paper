# it-sig

A simple Rust terminal tool demonstrating digital signature concepts using finite field algebra.


## Building

```bash
cargo build --release
```

## Running

```bash
cargo run
# or
cargo run -- "my-secret-seed" "my-message"
```

Or run the release binary:

```bash
./target/release/it-sig
```

